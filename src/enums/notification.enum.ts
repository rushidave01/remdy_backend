export enum user_notification_type {
  patient = "PATIENT",
  doctor = "DOCTOR",
}

export enum notification_status {
  sent = "SENT",
  failed = "FAILED",
  pending = "PENDING",
}

export enum notification_type {
  push = "PUSH",
  normal = "NORMAL",
}
